# Forge Signal Demo

Public landing-page demo for [`forge-signal-wasm`](https://www.npmjs.com/package/forge-signal-wasm).

## Run Locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The GitHub Pages workflow builds the Vite app and publishes `dist/` from the `main` branch.
